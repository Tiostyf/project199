{
  "name": "aiims-fullstack-app",
  "version": "1.0.0",
  "description": "AIIMS appointment system with Express backend and frontend",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "install-all": "npm install && cd backend && npm install",
    "build": "cd backend && npm install",
    "render-build": "npm run install-all"
  },
  "keywords": [
    "express",
    "mongodb",
    "authentication",
    "healthcare",
    "aiims"
  ],
  "author": "Developer",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  }
}